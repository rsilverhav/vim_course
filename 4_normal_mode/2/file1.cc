void randomFunction() {
  std::string str = "this is an important string";

  if (str == "always false") {
    // This is a comment that says nothing about the code, lets keep it
    const int i = 5 % CONST_INT;
    someVector.push_back(i);
    print(str);
  }
}
